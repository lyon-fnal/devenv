# Source this script within the container to restore
# UPS' setup and unsetup shell functions

setup () {
 source `ups setup "$@"`
}

unsetup () {
  source `ups unsetup "$@"`
}

mrbsetenv () {
    source ${MRB_DIR}/bin/mrbSetEnv
}
